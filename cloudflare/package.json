{
  "name": "node-center-cloudflare-tgbot",
  "version": "3.8.1",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "deploy:pages": "bash deploy-pages-gateway.sh",
    "dev": "wrangler dev",
    "check": "node --check src/index.js && node --check src/groups.js && node --check 'pages-gateway/functions/[[path]].js'",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "4.129.0"
  }
}
